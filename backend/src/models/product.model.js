const { query } = require('../config/db');
async function list(filters = {}) { const params = []; const conditions = []; if (filters.category_id) { conditions.push('p.category_id = ?'); params.push(filters.category_id); } if (filters.status) { conditions.push('p.status = ?'); params.push(filters.status); } const where = conditions.length ? `WHERE ${conditions.join(' AND ')}` : ''; return query(`SELECT p.*, pc.name AS category_name FROM products p JOIN product_categories pc ON pc.id = p.category_id ${where} ORDER BY p.name`, params); }
async function findById(id) { const products = await query('SELECT p.*, pc.name AS category_name FROM products p JOIN product_categories pc ON pc.id = p.category_id WHERE p.id = ?', [id]); if (!products[0]) return null; products[0].variants = await query('SELECT * FROM product_variants WHERE product_id = ?', [id]); return products[0]; }
async function create(data) { const result = await query('INSERT INTO products (category_id, name, sku, description, unit, base_price, cost_price, tax_percent, billing_type, status) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)', [data.category_id, data.name, data.sku, data.description || null, data.unit || 'UNIT', data.base_price, data.cost_price, data.tax_percent || 0, data.billing_type || 'ONE_TIME', data.status || 'ACTIVE']); return findById(result.insertId); }
async function update(id, data) { await query('UPDATE products SET category_id = COALESCE(?, category_id), name = COALESCE(?, name), sku = COALESCE(?, sku), description = COALESCE(?, description), unit = COALESCE(?, unit), base_price = COALESCE(?, base_price), cost_price = COALESCE(?, cost_price), tax_percent = COALESCE(?, tax_percent), billing_type = COALESCE(?, billing_type), status = COALESCE(?, status) WHERE id = ?', [data.category_id ?? null, data.name ?? null, data.sku ?? null, data.description ?? null, data.unit ?? null, data.base_price ?? null, data.cost_price ?? null, data.tax_percent ?? null, data.billing_type ?? null, data.status ?? null, id]); return findById(id); }
async function remove(id) { const result = await query('UPDATE products SET status = \'INACTIVE\' WHERE id = ?', [id]); return result.affectedRows > 0; }
module.exports = { list, findById, create, update, remove };
